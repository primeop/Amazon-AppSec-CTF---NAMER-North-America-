<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Evil Core Employee Portal{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#16213E',
                        secondary: '#0F3460',
                        accent: '#E94560',
                        dark: '#1A1A2E',
                    }
                }
            }
        }
    </script>
    <style type="text/tailwindcss">
        @layer components {
            .btn-primary {
                @apply bg-accent text-white font-bold py-2 px-4 rounded-md hover:bg-opacity-90 transition-all;
            }
            .btn-secondary {
                @apply bg-secondary text-white font-bold py-2 px-4 rounded-md hover:bg-opacity-90 transition-all;
            }
            .input-field {
                @apply border border-gray-300 p-2 rounded-md focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent;
            }
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
    {% if user is defined %}
    <nav class="bg-primary text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="flex space-x-6">
                <a href="/dashboard" class="font-bold text-xl">Evil Core Employee Portal</a>
                <div class="flex items-center space-x-4">
                    <a href="/dashboard" class="text-white hover:text-accent">Dashboard</a>
                    <a href="/self-review" class="text-white hover:text-accent">Self Review</a>
                </div>
            </div>
            <div class="flex items-center space-x-4">
                <span>Welcome, {{ user.firstName }}</span>
                <a href="/logout" class="bg-accent px-3 py-1 rounded-md text-sm">Logout</a>
            </div>
        </div>
    </nav>
    {% endif %}

    <main class="container mx-auto py-8 px-4">
        {% block content %}{% endblock %}
    </main>

    <footer class="mt-auto bg-primary text-white p-4 mt-auto">
        <div class="container mx-auto text-center">
            <p>© {{ "now"|date("Y") }} Evil Core. All rights reserved.</p>
        </div>
    </footer>
</body>
</html> 